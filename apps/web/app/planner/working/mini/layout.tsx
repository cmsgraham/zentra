export const metadata = {
  title: 'Mini Working Mode | Zentra',
  description: 'Compact execution view',
};

export default function MiniWorkingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
